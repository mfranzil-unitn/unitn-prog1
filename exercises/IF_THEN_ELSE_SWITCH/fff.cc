using namespace std;
#include <iostream>

int main () 
{ 
  int x, y, z;
  
  cout << "Dammi 3 interi x, y e z: ";
  cin >> x >> y >> z;
  
  if (x<y) 
    if (x<z) 
      cout << "il minimo e'" << x << endl; // (x<y)&&(x<z) 
    else // (x<y)&&(x>=z)
      
       
return 0;
}
